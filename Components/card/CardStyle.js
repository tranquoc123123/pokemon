import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 100,
    width: 50,
  },
  tinyLogo: {
    height: "100%",
    width: "70%"
  }
})

export default styles;